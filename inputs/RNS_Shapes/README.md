# RNS surfaces

Place manually supplied tabulated RNS surfaces here. Each CSV file must contain the columns `Mu` and `Radius (KM)`.

The full-grid script stores its surfaces in the selected output directory and passes that location to NS-SWORD. You should specify the directory to be this directory. 